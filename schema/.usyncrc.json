{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/uber-workflow/usync/blob/v0.0.0/schema/.usyncrc.json",
  "type": "object",
  "properties": {
    "mapping": {
      "type": "object",
      "description": "A map of external repo names to path maps",
      "additionalProperties": {
        "type": "object",
        "description": "A map of monorepo paths to external repo paths"
      }
    }
  },
  "required": [
    "mapping"
  ]
}
